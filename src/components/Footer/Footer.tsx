import { Heading, VStack } from "@chakra-ui/react"

export const Footer = () => {
    return (
        <VStack padding={3}>
            <Heading as={'h2'} size={'md'}>Todos os direitos reservados</Heading>
        </VStack>
    )
}